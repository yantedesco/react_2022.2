export const Button = () => {
  const estiloBotao = {
    backgroundColor: 'red',
    padding: '4px 12px',
    fontSize: 12
  }

  return (
    <button style={estiloBotao} onClick={() => null}>Botão</button>
  )
}